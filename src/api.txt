// run tw and overwrite cache
tw -n, --invalidate-cache

// setup
// gets api key, basic format config
tw configure

// update format
tw format <format string>

// print out info
tw info

Usage
  $ tw <command> [options]

Commands
  configure
  
  

Options
  --invalidate-cache, -n
  --prompt, -p

Examples
  
  $ tw configure 
  $ tw format 'i ' 
  $ tw info
  $ tw -np


command arg
option
  
